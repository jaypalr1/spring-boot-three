extend type Query {
    getBookData(id: ID): Book     # ! - means not null
#    bookById(id: ID!): [Book]    # Returns a list
}

type Book {
    id: ID
    name: String
    pageCount: Int
    author: Author
}

type Author {
    id: ID
    firstName: String
    lastName: String
}
